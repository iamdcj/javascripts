//* ==== Parse Float ====
//* - Takes a string as a param and attempts to
//* convert it to a decimal number
let numStr = "123.4";
numStr = parseFloat(numStr)
console.log(numStr, typeof numStr); 

//{} 123.4 'number'

